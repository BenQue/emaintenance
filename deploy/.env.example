# E-Maintenance Docker部署环境变量配置
# 复制此文件为 .env 并根据实际环境修改

# 数据库配置
POSTGRES_PASSWORD=Qzy@7091!

# JWT密钥
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# 环境模式
NODE_ENV=development

# API网关配置（可选）
# 如果留空，Web应用将使用相对路径通过Nginx代理访问API
# 示例配置：
# - 局域网访问: API_GATEWAY_URL=http://192.168.1.100
# - 域名访问: API_GATEWAY_URL=https://emaintenance.example.com
# - Docker内部: API_GATEWAY_URL=http://nginx
API_GATEWAY_URL=

# 单独服务URL配置（可选，通常留空）
# 只有在需要直接访问特定服务时才配置
API_URL=
USER_SERVICE_URL=
WORK_ORDER_SERVICE_URL=
ASSET_SERVICE_URL=

# 移动端API配置
# 用于移动端访问的服务器地址
# 示例：
# - 局域网: MOBILE_API_HOST=192.168.1.100
# - 公网域名: MOBILE_API_HOST=emaintenance.example.com
MOBILE_API_HOST=localhost
MOBILE_API_PORT=80
MOBILE_API_PROTOCOL=http